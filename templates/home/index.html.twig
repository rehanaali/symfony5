{% extends 'base.html.twig' %}

{% block title %}{{ title }}!{% endblock %}

{% block body %}

        <div class="row align-items-center h-100">
            <div class="col-6 mx-auto">
                <div class="jumbotron text-center">
                    <h1>{{ title }}</h1>

                    <a href="{{ path('todo') }}" class="btn btn-sm btn-outline-primary">
                        MyTodoList
                    </a>
                </div>
            </div>
        </div>


{% endblock %}




